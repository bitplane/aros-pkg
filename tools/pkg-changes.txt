pkg U looks before it asks: when pkg is already the newest, it says so and asks for nothing; when a newer one is offered and replacing it needs administrator access, the question names both versions. Declining is not an error: it says what stays on offer.
